// Interface defining bookable actions for an appointment
public interface Bookings {
    void confirmBooking();  // Confirms a new booking
    void cancelBooking();   // Cancels an existing booking
}
